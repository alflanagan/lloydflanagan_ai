# Design Goals

The goals for this design are:

- simplicity and readability — a clean layout that stays out of the way of the content.
- fast page loads by avoiding unnecessary javascript.
- a consistent visual language throughout via a shared component library.
- a responsive design that works comfortably on any screen size.
