# Claude AI Prompts

1. Create a starter website in ~/Devel/personal/web/ai/lloydflanagan which
   uses python for the back end and lit with shoelace for the front end. It
   should have a home page with "A Lloyd Flanagan" as a header, and a menu
   with links "About Me", "Blog", "Education", each of which links to an
   empty page.
2. Add a file to the project called PROMPTS.md. Write a markdown list with
   the heading "Claude AI Prompts" and each prompt for this session.
3. Modify the PROMPTS.md file to wrap text at column 80, and append this
   prompt to the list.
4. Convert the header to use font "Oooh Baby" and add this prompt to
   PROMPTS.md.
5. Replace the font "Oooh Baby" with "Playwrite New Zealand Basic" and write
   this prompt to PROMPTS.md.
6. Center the name in the center of the page and increase the font size by
   50%.
7. Add standard spacing between the menu links, and to the bottom margin.
8. Add a paragraph after the Welcome message that says "This is an AI
   experiment &mdash; all code was generated from AI (Claude 4.6)."
9. Add a new menu item "Prompts" which links to a new page that displays the
   prompts from PROMPTS.md in an attractive layout.
10. Debug and fix the error that menu links don't link to their targets.
11. Add README.md document which describes the project in detail. Include a
   badge for this AI model.
12. Add a link to the GitHub repository in the upper right of the header.
13. Add a Home link to the menu, and make the name in the header be a link
   to the home page.
14. fix error "Could not find FastAPI app in module, try using --app"
15. In README.md file, at the end of "generated by Claude 4.6", add a
   footnote: "Except for certain files related to deployment.".
16. Add a gitlab workflow to deploy to fly.io when I push to main branch.
17. Add alloydflanagan.com to the URL whitelist.
18. add lloydflanagan.fly.dev to allowed hosts
19. Create a new page at URL design/ with the title "Design Notes". Add a
   link from the home page under the github logo labelled "Design". On the
   first card on the page, write a short paragraph listing the design goals
   for the site. In another card, list each of the major third-party
   software products used with a short description of why they were chosen.
   Add this prompt to PROMPTS.md.
20. Add more space between the github logo and the Design link, using a
   shoelace spacing variable. Change the color of the link to use a light
   shoelace accent color.
21. For each software name in the second card of the design page, change the
   name into a link to the tool's main web site.
22. Replace the label on the Home link with an icon used to signify "Home".
23. Fix color contrast problem with the home icon.
24. Create a new component derived from sl-card which outputs an sl-card
   with contents converted to html from an external markdown file stored in
   a content subdirectory.
25. Correct dockerfile to use uv for python install and copy uv.lock.
26. Create a requirements document in REQUIREMENTS.md. It should include an
   "about me" page, a professional blog, and a personal blog. Analyze
   existing code for additional requirements.
27. /root:docs scan — discover undocumented components and generate docs for
   src/lloydflanagan/app.py, static/js/site-header.js, and
   static/js/markdown-card.js.
28. /root:prd about-me — create a PRD for the About Me page
   (docs/prds/about-me.md).
29. reformat PROMPTS.md
30. Create a Dockerfile: first build to copy the app to a directory, and
   build it, second build copies the production files from first build and
   runs a server to serve the site on localhost:8000
31. update .dockerignore file to include anything not needed in the docker
   image.
32. set up basic gitlab workflow to run ruff with python files (except those
   in .gitignored directories or the migrations/ directories). Have it
   disallow merge if any file does not pass ruff.
33. I need to update the fly.toml in the docker image to use the value of
   the environment variable FLY\_API\_TOKEN as the access token
34. remember that when a python file is changed, you need to run 'ruff
   check' on it. If errors are found, ask me if ruff should correct them.
   If I say it should, use 'ruff check --fix' and'ruff format' on the files.
35. the fix worked, deploy now works. However, the app needs to connect to
   external port 8000, not 8080. implement a fix for that
36. analyze project and update CLAUDE.md as needed
37. implement suggested fix for "uv run dev" alias
38. Set this image up as background image for all pages. [parchment/grunge
   paper texture]
39. create a file in static/css called variables.css. implement the
   complete palette in this as css variables with descriptive names.
40. add a comment to @variables.css listing the sources you consulted to
   build the palette:
41. I've updated the project to use yarn as a package manager for the
   project and added lit as a dependency. rescan the project and update
   CLAUDE.md as needed.
42. write a commit message for staged changes
43. add an appropriate favicon.ico file
44. create a file in @/Users/adrianflanagan/Devel/personal/web/ai/lloydflanagan/content
   directory named colors.md. Using markdown format, create content for the
   file using the response you gave when i requested a new palette. include,
   if possible, styles referencing @static/css/variables.css variables
   instead of hard-coding values.
45. check the markdown file @INDEX.md for spelling, grammar errors, and
   suggest improvements.
46. fix all the issues
47. Analyze changed files and update
   @/Users/adrianflanagan/Devel/personal/web/ai/lloydflanagan/CLAUDE.md as
   needed.
48. Update context from changed files and update CLAUDE.md.
